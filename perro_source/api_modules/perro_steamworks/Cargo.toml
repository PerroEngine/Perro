[package]
name = "perro_steamworks"
version = "0.1.0"
edition = "2024"

[dependencies]
steamworks = { version = "0.13.1", optional = true }

[features]
default = []
steamworks-runtime = ["dep:steamworks"]
