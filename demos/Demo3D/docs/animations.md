# Animation Demo

This scene uses `AnimationPlayer` nodes bound to `.panim` clips:

- `res://animations/demo_bob.panim`
- `res://animations/demo_pulse.panim`

Each clip declares `Hero = Node3D`.
Scene bindings map `Hero` to each visible mesh root.
