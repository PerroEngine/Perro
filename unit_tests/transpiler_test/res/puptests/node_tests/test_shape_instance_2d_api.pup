// Compile-only test: ShapeInstance2D API (shape, color, filled) + Node2D.
@script TestShapeInstance2DApi extends ShapeInstance2D

on init() {
    self.shape = Shape.rectangle(32.0, 32.0)
    self.shape = Shape.circle(16.0)
    self.shape = Shape.square(24.0)
    self.shape = Shape.triangle(10.0, 20.0)
    var c = self.color
    self.filled = true
    self.transform.position = new Vector2(0.0, 0.0)
    var n = self.name
}
