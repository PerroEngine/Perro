#![allow(improper_ctypes_definitions)]
#![allow(unused)]
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]

use std::{
    any::Any,
    borrow::Cow,
    cell::RefCell,
    collections::HashMap,
    ops::{Deref, DerefMut},
    rc::Rc,
    str::FromStr,
};

use num_bigint::BigInt;
use phf::{phf_map, Map};
use rust_decimal::Decimal;
use rust_decimal::prelude::{FromPrimitive, ToPrimitive};
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use smallvec::{smallvec, SmallVec};
use perro_core::{TextureID, NodeID, MaterialID, MeshID, LightID, UIElementID, SignalID};

use perro_core::nodes::ui::ui_registry::UIElementType;
use perro_core::nodes::ui::ui_elements::{ui_button, ui_text};

use perro_core::prelude::*;

//=======================================;
// Auto Generated by Perro Transpiler [Any further edits to this file will be overwritten on next transile];
//=======================================;

const __PERRO_SOURCE_FILE: &str = "root.pup";

// ========================================================================
// PuptestsGlobalTestsRootPup - Main Script Structure
// ========================================================================

static MEMBER_TO_ATTRIBUTES_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

static ATTRIBUTE_TO_MEMBERS_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

pub struct PuptestsGlobalTestsRootPupScript {
    id: NodeID,
    __t_root_counter: i32,
}

// ========================================================================
// PuptestsGlobalTestsRootPup - Creator Function (FFI Entry Point)
// ========================================================================

#[unsafe(no_mangle)]
pub extern "C" fn puptests_global_tests_root_pup_create_script() -> *mut dyn ScriptObject {
    let id = NodeID::nil(); // Will be set when attached to node
    let root_counter = 0i32;

    Box::into_raw(Box::new(PuptestsGlobalTestsRootPupScript {
        id,
        __t_root_counter: root_counter,
    })) as *mut dyn ScriptObject
}

// ========================================================================
// PuptestsGlobalTestsRootPup - Script Init & Update Implementation
// ========================================================================

impl Script for PuptestsGlobalTestsRootPupScript {
    fn init(&mut self, api: &mut ScriptApi<'_>) {
        self.__t_root_counter = 1i32;
    }

}

// ========================================================================
// PuptestsGlobalTestsRootPup - Script-Defined Methods
// ========================================================================

impl PuptestsGlobalTestsRootPupScript {
    fn __t_get_root_value(&mut self, api: &mut ScriptApi<'_>) -> i32 {
return 100i32;    0i32
    }

    fn __t_root_uses_module(&mut self, api: &mut ScriptApi<'_>) -> f32 {
return crate::puptests_module_tests_math_utils_pup::MathUtils::__t_scale(1.0f32, crate::puptests_module_tests_math_utils_pup::MathUtils::__t_PI, api);    0.0f32
    }

}


impl ScriptObject for PuptestsGlobalTestsRootPupScript {
    fn set_id(&mut self, id: NodeID) {
        self.id = id;
    }

    fn get_id(&self) -> NodeID {
        self.id
    }

    fn get_var(&self, var_id: u64) -> Option<Value> {
        VAR_GET_TABLE.get(&var_id).and_then(|f| f(self))
    }

    fn set_var(&mut self, var_id: u64, val: Value) -> Option<()> {
        VAR_SET_TABLE.get(&var_id).and_then(|f| f(self, val))
    }

    fn apply_exposed(&mut self, hashmap: &HashMap<u64, Value>) {
        for (var_id, val) in hashmap.iter() {
            if let Some(f) = VAR_APPLY_TABLE.get(var_id) {
                f(self, val);
            }
        }
    }

    fn call_function(
        &mut self,
        id: u64,
        api: &mut ScriptApi<'_>,
        params: &[Value],
    ) -> Value {
        DISPATCH_TABLE.get(&id).map_or(Value::Null, |f| f(self, params, api))
    }

    // Attributes

    fn attributes_of(&self, member: &str) -> Vec<String> {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn members_with(&self, attribute: &str) -> Vec<String> {
        ATTRIBUTE_TO_MEMBERS_MAP
            .get(attribute)
            .map(|members| members.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn has_attribute(&self, member: &str, attribute: &str) -> bool {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().any(|a| *a == attribute))
            .unwrap_or(false)
    }
    
    fn script_flags(&self) -> ScriptFlags {
        ScriptFlags::new(1)
    }
}

// =========================== Static PHF Dispatch Tables ===========================

static VAR_GET_TABLE: phf::Map<u64, fn(&PuptestsGlobalTestsRootPupScript) -> Option<Value>> =
    phf::phf_map! {
        18206167897987445329u64 => |script: &PuptestsGlobalTestsRootPupScript| -> Option<Value> {
                        Some(json!(script.__t_root_counter))
                    },

    };

static VAR_SET_TABLE: phf::Map<u64, fn(&mut PuptestsGlobalTestsRootPupScript, Value) -> Option<()>> =
    phf::phf_map! {
        18206167897987445329u64 => |script: &mut PuptestsGlobalTestsRootPupScript, val: Value| -> Option<()> {
                            if let Some(v) = val.as_i64() {
                                script.__t_root_counter = v as i32;
                                return Some(());
                            }
                            None
                        },

    };

static VAR_APPLY_TABLE: phf::Map<u64, fn(&mut PuptestsGlobalTestsRootPupScript, &Value)> =
    phf::phf_map! {

    };

static DISPATCH_TABLE: phf::Map<
    u64,
    fn(&mut PuptestsGlobalTestsRootPupScript, &[Value], &mut ScriptApi<'_>) -> Value,
> = phf::phf_map! {
        11967537757100617670u64 => | script: &mut PuptestsGlobalTestsRootPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            json!(script.__t_get_root_value(api))
        },
        15353357115589987819u64 => | script: &mut PuptestsGlobalTestsRootPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            json!(script.__t_root_uses_module(api))
        },

    };
