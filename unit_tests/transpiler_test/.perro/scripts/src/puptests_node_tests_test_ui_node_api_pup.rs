#![allow(improper_ctypes_definitions)]
#![allow(unused)]
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]

use std::{
    any::Any,
    borrow::Cow,
    cell::RefCell,
    collections::HashMap,
    ops::{Deref, DerefMut},
    rc::Rc,
    str::FromStr,
};

use num_bigint::BigInt;
use phf::{phf_map, Map};
use rust_decimal::Decimal;
use rust_decimal::prelude::{FromPrimitive, ToPrimitive};
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use smallvec::{smallvec, SmallVec};
use perro_core::{TextureID, NodeID, MaterialID, MeshID, LightID, UIElementID, SignalID};

use perro_core::nodes::ui::ui_registry::UIElementType;
use perro_core::nodes::ui::ui_elements::{ui_button, ui_text};

use perro_core::prelude::*;

//=======================================;
// Auto Generated by Perro Transpiler [Any further edits to this file will be overwritten on next transile];
//=======================================;

const __PERRO_SOURCE_FILE: &str = "test_ui_node_api.pup";

// ========================================================================
// PuptestsNodeTestsTestUiNodeApiPup - Main Script Structure
// ========================================================================

static MEMBER_TO_ATTRIBUTES_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

static ATTRIBUTE_TO_MEMBERS_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

pub struct PuptestsNodeTestsTestUiNodeApiPupScript {
    id: NodeID,
}

// ========================================================================
// PuptestsNodeTestsTestUiNodeApiPup - Creator Function (FFI Entry Point)
// ========================================================================

#[unsafe(no_mangle)]
pub extern "C" fn puptests_node_tests_test_ui_node_api_pup_create_script() -> *mut dyn ScriptObject {
    let id = NodeID::nil(); // Will be set when attached to node

    Box::into_raw(Box::new(PuptestsNodeTestsTestUiNodeApiPupScript {
        id,
    })) as *mut dyn ScriptObject
}

// ========================================================================
// PuptestsNodeTestsTestUiNodeApiPup - Script Init & Update Implementation
// ========================================================================

impl Script for PuptestsNodeTestsTestUiNodeApiPupScript {
    fn init(&mut self, api: &mut ScriptApi<'_>) {
        self.__t_test_get_element(api);
        self.__t_test_get_element_cast_typed(api);
        self.__t_test_dyn_element(api);
        self.__t_test_typed_no_null_check(api);
        self.__t_test_dyn_no_null_check(api);
    }

}

// ========================================================================
// PuptestsNodeTestsTestUiNodeApiPup - Script-Defined Methods
// ========================================================================

impl PuptestsNodeTestsTestUiNodeApiPupScript {
    fn __t_test_get_element(&mut self, api: &mut ScriptApi<'_>) {
        let mut el_id: Option<UIElementID> = api.get_element_by_name(self.id, "SomeButton");
        if el_id.is_some() {
            let mut _dummy_id: Option<UIElementID> = el_id.clone();
        }
        let mut missing_id: Option<UIElementID> = api.get_element_by_name(self.id, "NonExistent");
        if missing_id.is_none() {
            let mut __t__ok: bool = true;
        }
    }

    fn __t_test_get_element_cast_typed(&mut self, api: &mut ScriptApi<'_>) {
        let mut text_el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MyText");
        if text_el_id.is_some() {
            let mut __t_c: String = api.read_ui_element(self.id, text_el_id.unwrap_or(UIElementID::nil()), |e: &ui_text::UIText| e.props.content.clone());
            let mut __t__t: String = __t_c.clone().clone();
            api.mutate_ui_element(self.id, text_el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_text::UIText| {
                e.props.content = String::from("Updated text");
            });
        }
        let mut btn_el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MyButton");
        if btn_el_id.is_some() {
            let mut __t_lbl: String = api.read_ui_element(self.id, btn_el_id.unwrap_or(UIElementID::nil()), |e: &ui_button::UIButton| e.label.clone());
            let mut __t__b: String = __t_lbl.clone().clone();
            api.mutate_ui_element(self.id, btn_el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_button::UIButton| {
                e.label = String::from("New label");
            });
        }
        let mut panel_el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MyPanel");
        if panel_el_id.is_some() {
            let mut _p_id: Option<UIElementID> = panel_el_id.clone();
        }
    }

    fn __t_test_dyn_element(&mut self, api: &mut ScriptApi<'_>) {
        let mut el_id: Option<UIElementID> = api.get_element_by_name(self.id, "Anything");
        if el_id.is_some() {
            let mut _held_id: Option<UIElementID> = el_id.clone();
            let mut __t_c: String = api.read_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &ui_text::UIText| e.props.content.clone());
            let mut __t__c2: String = __t_c.clone().clone();
            api.mutate_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_text::UIText| {
                e.props.content = String::from("Dyn content");
            });
            let mut __t_lbl: String = api.read_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &ui_button::UIButton| e.label.clone());
            let mut __t__l2: String = __t_lbl.clone().clone();
            api.mutate_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_button::UIButton| {
                e.label = String::from("Dyn label");
            });
        }
    }

    fn __t_test_typed_no_null_check(&mut self, api: &mut ScriptApi<'_>) {
        let mut text_el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MaybeMissing");
        let mut __t_c: String = api.read_ui_element(self.id, text_el_id.unwrap_or(UIElementID::nil()), |e: &ui_text::UIText| e.props.content.clone());
        let mut __t__t: String = __t_c.clone().clone();
        api.mutate_ui_element(self.id, text_el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_text::UIText| {
            e.props.content = String::from("Set anyway");
        });
        let mut btn_el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MaybeMissingBtn");
        let mut __t_lbl: String = api.read_ui_element(self.id, btn_el_id.unwrap_or(UIElementID::nil()), |e: &ui_button::UIButton| e.label.clone());
        let mut __t__b: String = __t_lbl.clone().clone();
        api.mutate_ui_element(self.id, btn_el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_button::UIButton| {
            e.label = String::from("Set anyway");
        });
    }

    fn __t_test_dyn_no_null_check(&mut self, api: &mut ScriptApi<'_>) {
        let mut el_id: Option<UIElementID> = api.get_element_by_name(self.id, "MaybeMissing");
        let mut __t_c: String = api.read_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &ui_text::UIText| e.props.content.clone());
        let mut __t__c: String = __t_c.clone().clone();
        api.mutate_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_text::UIText| {
            e.props.content = String::from("Set anyway");
        });
        let mut __t_lbl: String = api.read_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &ui_button::UIButton| e.label.clone());
        let mut __t__l: String = __t_lbl.clone().clone();
        api.mutate_ui_element(self.id, el_id.unwrap_or(UIElementID::nil()), |e: &mut ui_button::UIButton| {
            e.label = String::from("Set anyway");
        });
    }

}


impl ScriptObject for PuptestsNodeTestsTestUiNodeApiPupScript {
    fn set_id(&mut self, id: NodeID) {
        self.id = id;
    }

    fn get_id(&self) -> NodeID {
        self.id
    }

    fn get_var(&self, var_id: u64) -> Option<Value> {
        VAR_GET_TABLE.get(&var_id).and_then(|f| f(self))
    }

    fn set_var(&mut self, var_id: u64, val: Value) -> Option<()> {
        VAR_SET_TABLE.get(&var_id).and_then(|f| f(self, val))
    }

    fn apply_exposed(&mut self, hashmap: &HashMap<u64, Value>) {
        for (var_id, val) in hashmap.iter() {
            if let Some(f) = VAR_APPLY_TABLE.get(var_id) {
                f(self, val);
            }
        }
    }

    fn call_function(
        &mut self,
        id: u64,
        api: &mut ScriptApi<'_>,
        params: &[Value],
    ) -> Value {
        DISPATCH_TABLE.get(&id).map_or(Value::Null, |f| f(self, params, api))
    }

    // Attributes

    fn attributes_of(&self, member: &str) -> Vec<String> {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn members_with(&self, attribute: &str) -> Vec<String> {
        ATTRIBUTE_TO_MEMBERS_MAP
            .get(attribute)
            .map(|members| members.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn has_attribute(&self, member: &str, attribute: &str) -> bool {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().any(|a| *a == attribute))
            .unwrap_or(false)
    }
    
    fn script_flags(&self) -> ScriptFlags {
        ScriptFlags::new(1)
    }
}

// =========================== Static PHF Dispatch Tables ===========================

static VAR_GET_TABLE: phf::Map<u64, fn(&PuptestsNodeTestsTestUiNodeApiPupScript) -> Option<Value>> =
    phf::phf_map! {

    };

static VAR_SET_TABLE: phf::Map<u64, fn(&mut PuptestsNodeTestsTestUiNodeApiPupScript, Value) -> Option<()>> =
    phf::phf_map! {

    };

static VAR_APPLY_TABLE: phf::Map<u64, fn(&mut PuptestsNodeTestsTestUiNodeApiPupScript, &Value)> =
    phf::phf_map! {

    };

static DISPATCH_TABLE: phf::Map<
    u64,
    fn(&mut PuptestsNodeTestsTestUiNodeApiPupScript, &[Value], &mut ScriptApi<'_>) -> Value,
> = phf::phf_map! {
        2482069085599037349u64 => | script: &mut PuptestsNodeTestsTestUiNodeApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_get_element(api);
            Value::Null
        },
        12813305073074166825u64 => | script: &mut PuptestsNodeTestsTestUiNodeApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_get_element_cast_typed(api);
            Value::Null
        },
        11777556591178843909u64 => | script: &mut PuptestsNodeTestsTestUiNodeApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_dyn_element(api);
            Value::Null
        },
        13553137765259467675u64 => | script: &mut PuptestsNodeTestsTestUiNodeApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_typed_no_null_check(api);
            Value::Null
        },
        12525108330135870185u64 => | script: &mut PuptestsNodeTestsTestUiNodeApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_dyn_no_null_check(api);
            Value::Null
        },

    };
