#![allow(improper_ctypes_definitions)]
#![allow(unused)]
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]

use std::{
    any::Any,
    borrow::Cow,
    cell::RefCell,
    collections::HashMap,
    ops::{Deref, DerefMut},
    rc::Rc,
    str::FromStr,
};

use num_bigint::BigInt;
use phf::{phf_map, Map};
use rust_decimal::Decimal;
use rust_decimal::prelude::{FromPrimitive, ToPrimitive};
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use smallvec::{smallvec, SmallVec};
use perro_core::{TextureID, NodeID, MaterialID, MeshID, LightID, UIElementID, SignalID};

use perro_core::prelude::*;

//=======================================;
// Auto Generated by Perro Transpiler [Any further edits to this file will be overwritten on next transile];
//=======================================;

const __PERRO_SOURCE_FILE: &str = "game_state.pup";

// ========================================================================
// PuptestsGlobalTestsGameStatePup - Main Script Structure
// ========================================================================

static MEMBER_TO_ATTRIBUTES_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

static ATTRIBUTE_TO_MEMBERS_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

pub struct PuptestsGlobalTestsGameStatePupScript {
    id: NodeID,
    __t_score: i32,
}

// ========================================================================
// PuptestsGlobalTestsGameStatePup - Creator Function (FFI Entry Point)
// ========================================================================

#[unsafe(no_mangle)]
pub extern "C" fn puptests_global_tests_game_state_pup_create_script() -> *mut dyn ScriptObject {
    let id = NodeID::nil(); // Will be set when attached to node
    let score = 0i32;

    Box::into_raw(Box::new(PuptestsGlobalTestsGameStatePupScript {
        id,
        __t_score: score,
    })) as *mut dyn ScriptObject
}

// ========================================================================
// PuptestsGlobalTestsGameStatePup - Script Init & Update Implementation
// ========================================================================

impl Script for PuptestsGlobalTestsGameStatePupScript {
    fn init(&mut self, api: &mut ScriptApi<'_>) {
        self.__t_score = 0i32;
    }

}

// ========================================================================
// PuptestsGlobalTestsGameStatePup - Script-Defined Methods
// ========================================================================

impl PuptestsGlobalTestsGameStatePupScript {
    fn __t_get_score(&mut self, api: &mut ScriptApi<'_>) -> i32 {
return self.__t_score;    0i32
    }

    fn __t_set_score(&mut self, mut __t_v: i32, api: &mut ScriptApi<'_>) {
        self.__t_score = __t_v;
    }

    fn __t_get_root_value_via_root(&mut self, api: &mut ScriptApi<'_>) -> i32 {
return (api.call_function_id(NodeID::from_u32(1), 11967537757100617670u64, &[]).as_i64().unwrap_or(0) as i32);    0i32
    }

    fn __t_game_uses_math(&mut self, api: &mut ScriptApi<'_>) -> i32 {
return crate::puptests_global_tests_math_utils_pup::MathUtils::__t_add(10i32, 20i32, api);    0i32
    }

}


impl ScriptObject for PuptestsGlobalTestsGameStatePupScript {
    fn set_id(&mut self, id: NodeID) {
        self.id = id;
    }

    fn get_id(&self) -> NodeID {
        self.id
    }

    fn get_var(&self, var_id: u64) -> Option<Value> {
        VAR_GET_TABLE.get(&var_id).and_then(|f| f(self))
    }

    fn set_var(&mut self, var_id: u64, val: Value) -> Option<()> {
        VAR_SET_TABLE.get(&var_id).and_then(|f| f(self, val))
    }

    fn apply_exposed(&mut self, hashmap: &HashMap<u64, Value>) {
        for (var_id, val) in hashmap.iter() {
            if let Some(f) = VAR_APPLY_TABLE.get(var_id) {
                f(self, val);
            }
        }
    }

    fn call_function(
        &mut self,
        id: u64,
        api: &mut ScriptApi<'_>,
        params: &[Value],
    ) -> Value {
        DISPATCH_TABLE.get(&id).map_or(Value::Null, |f| f(self, params, api))
    }

    // Attributes

    fn attributes_of(&self, member: &str) -> Vec<String> {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn members_with(&self, attribute: &str) -> Vec<String> {
        ATTRIBUTE_TO_MEMBERS_MAP
            .get(attribute)
            .map(|members| members.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn has_attribute(&self, member: &str, attribute: &str) -> bool {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().any(|a| *a == attribute))
            .unwrap_or(false)
    }
    
    fn script_flags(&self) -> ScriptFlags {
        ScriptFlags::new(1)
    }
}

// =========================== Static PHF Dispatch Tables ===========================

static VAR_GET_TABLE: phf::Map<u64, fn(&PuptestsGlobalTestsGameStatePupScript) -> Option<Value>> =
    phf::phf_map! {
        953527838510411331u64 => |script: &PuptestsGlobalTestsGameStatePupScript| -> Option<Value> {
                        Some(json!(script.__t_score))
                    },

    };

static VAR_SET_TABLE: phf::Map<u64, fn(&mut PuptestsGlobalTestsGameStatePupScript, Value) -> Option<()>> =
    phf::phf_map! {
        953527838510411331u64 => |script: &mut PuptestsGlobalTestsGameStatePupScript, val: Value| -> Option<()> {
                            if let Some(v) = val.as_i64() {
                                script.__t_score = v as i32;
                                return Some(());
                            }
                            None
                        },

    };

static VAR_APPLY_TABLE: phf::Map<u64, fn(&mut PuptestsGlobalTestsGameStatePupScript, &Value)> =
    phf::phf_map! {

    };

static DISPATCH_TABLE: phf::Map<
    u64,
    fn(&mut PuptestsGlobalTestsGameStatePupScript, &[Value], &mut ScriptApi<'_>) -> Value,
> = phf::phf_map! {
        7006042117341749029u64 => | script: &mut PuptestsGlobalTestsGameStatePupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            json!(script.__t_get_score(api))
        },
        13492554068693301597u64 => | script: &mut PuptestsGlobalTestsGameStatePupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
let __t_v = params.get(0)
                            .and_then(|v| v.as_i64().or_else(|| v.as_f64().map(|f| f as i64)))
                            .unwrap_or_default() as i32;
            script.__t_set_score(__t_v, api);
            Value::Null
        },
        5138814962466354492u64 => | script: &mut PuptestsGlobalTestsGameStatePupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            json!(script.__t_get_root_value_via_root(api))
        },
        18028419204930178484u64 => | script: &mut PuptestsGlobalTestsGameStatePupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            json!(script.__t_game_uses_math(api))
        },

    };
