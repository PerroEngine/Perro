#![allow(improper_ctypes_definitions)]
#![allow(unused)]
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]

use std::{
    any::Any,
    borrow::Cow,
    cell::RefCell,
    collections::HashMap,
    ops::{Deref, DerefMut},
    rc::Rc,
    str::FromStr,
};

use num_bigint::BigInt;
use phf::{phf_map, Map};
use rust_decimal::Decimal;
use rust_decimal::prelude::{FromPrimitive, ToPrimitive};
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use smallvec::{smallvec, SmallVec};
use perro_core::{TextureID, NodeID, MaterialID, MeshID, LightID, UIElementID};

use perro_core::prelude::*;

//=======================================;
// Auto Generated by Perro Transpiler [Any further edits to this file will be overwritten on next transile];
//=======================================;

const __PERRO_SOURCE_FILE: &str = "test_node3d_api.pup";

// ========================================================================
// PuptestsNodeTestsTestNode3dApiPup - Main Script Structure
// ========================================================================

static MEMBER_TO_ATTRIBUTES_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

static ATTRIBUTE_TO_MEMBERS_MAP: Map<&'static str, &'static [&'static str]> = phf_map! {
};

pub struct PuptestsNodeTestsTestNode3dApiPupScript {
    id: NodeID,
    __t_spin: f32,
}

// ========================================================================
// PuptestsNodeTestsTestNode3dApiPup - Creator Function (FFI Entry Point)
// ========================================================================

#[unsafe(no_mangle)]
pub extern "C" fn puptests_node_tests_test_node3d_api_pup_create_script() -> *mut dyn ScriptObject {
    let id = NodeID::nil(); // Will be set when attached to node
    let spin = 0.0f32;

    Box::into_raw(Box::new(PuptestsNodeTestsTestNode3dApiPupScript {
        id,
        __t_spin: spin,
    })) as *mut dyn ScriptObject
}

// ========================================================================
// PuptestsNodeTestsTestNode3dApiPup - Script Init & Update Implementation
// ========================================================================

impl Script for PuptestsNodeTestsTestNode3dApiPupScript {
    fn init(&mut self, api: &mut ScriptApi<'_>) {
        self.__t_test_transform_read_write(api);
        self.__t_test_rotation_assign_and_rotate(api);
        self.__t_test_rotation_set_equal_to_other(api);
        self.__t_test_for_loops_with_rotation(api);
        self.__t_test_scale_and_position_assign(api);
    }

}

// ========================================================================
// PuptestsNodeTestsTestNode3dApiPup - Script-Defined Methods
// ========================================================================

impl PuptestsNodeTestsTestNode3dApiPupScript {
    fn __t_test_transform_read_write(&mut self, api: &mut ScriptApi<'_>) {
        let mut __t_t: Transform3D = (api.read_node(self.id, |self_node: &Node3D| self_node.transform));
        let mut __t_pos: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.position));
        let mut __t_rot: Quaternion = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.rotation));
        let mut __t_scale: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.scale));
        api.mutate_node(self.id, |self_node: &mut Node3D| {
            self_node.transform.position = Vector3::new(1.0f32, 2.0f32, 3.0f32);
            self_node.transform.position.x = 10.0f32;
            self_node.transform.position.y = 20.0f32;
            self_node.transform.position.z = 30.0f32;
        });
        let __temp_api_0: Quaternion = Quaternion::identity();
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = __temp_api_0; });
        let __temp_api_0: Quaternion = Quaternion::from_euler_degrees(0.0f32, 90.0f32, 0.0f32);
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = __temp_api_0; });
        let mut __t_euler: Vector3 = ((api.read_node(self.id, |self_node: &Node3D| self_node.transform.rotation))).as_euler();
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.scale = Vector3::new(1.0f32, 1.0f32, 1.0f32); });
        let mut __t_gt: Transform3D = api.get_global_transform_3d(self.id).unwrap_or_default();
        let mut __t_pv: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.pivot));
        api.mutate_node(self.id, |self_node: &mut Node3D| {
            self_node.pivot = Vector3::new(0.0f32, 0.0f32, 0.0f32);
            self_node.visible = false;
        });
        let mut __t_n: Cow<'static, str> = api.read_scene_node(self.id, |n| Cow::Owned(n.get_name().to_string()));
    }

    fn __t_test_rotation_assign_and_rotate(&mut self, api: &mut ScriptApi<'_>) {
        let __temp_api_0: Quaternion = Quaternion::identity();
        api.mutate_node(self.id, |self_node: &mut Node3D| {
            self_node.transform.rotation = __temp_api_0;
            self_node.transform.rotation = (self_node.transform.rotation).rotate_x(45.0f32);
            self_node.transform.rotation = (self_node.transform.rotation).rotate_y(90.0f32);
            self_node.transform.rotation = (self_node.transform.rotation).rotate_z(15.0f32);
            self_node.transform.rotation = (self_node.transform.rotation).rotate_euler_degrees(1.0f32, 2.0f32, 3.0f32);
        });
        let mut __t_e: Vector3 = ((api.read_node(self.id, |self_node: &Node3D| self_node.transform.rotation))).as_euler();
    }

    fn __t_test_rotation_set_equal_to_other(&mut self, api: &mut ScriptApi<'_>) {
        let mut __t_q: Quaternion = Quaternion::from_euler_degrees(30.0f32, 60.0f32, 0.0f32);
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = __t_q; });
        let mut __t_other: Quaternion = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.rotation));
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = __t_other; });
        let mut __t_q2: Quaternion = (__t_q).rotate_y(10.0f32);
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = __t_q2; });
    }

    fn __t_test_for_loops_with_rotation(&mut self, api: &mut ScriptApi<'_>) {
        for __t_i in (0i32..36i32) {
            api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.rotation = (self_node.transform.rotation).rotate_y(10.0f32); });
            self.__t_spin = (self.__t_spin + 1.0f32);
        }
        for __t_j in (0i32..10i32) {
            let mut __t_pos: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.position));
            api.mutate_node(self.id, |self_node: &mut Node3D| {
                self_node.transform.position = Vector3::new(((__t_pos.x as f32) + 0.1f32), __t_pos.y, __t_pos.z);
                self_node.transform.rotation = (self_node.transform.rotation).rotate_x(5.0f32);
            });
        }
    }

    fn __t_test_scale_and_position_assign(&mut self, api: &mut ScriptApi<'_>) {
        let mut __t_s: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.scale));
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.scale = Vector3::new(2.0f32, 2.0f32, 2.0f32); });
        let mut __t_p: Vector3 = (api.read_node(self.id, |self_node: &Node3D| self_node.transform.position));
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.position = Vector3::new(__t_p.x, ((__t_p.y as f32) + 1.0f32), __t_p.z); });
        let mut __t_other_scale: Vector3 = Vector3::new(0.5f32, 0.5f32, 0.5f32);
        api.mutate_node(self.id, |self_node: &mut Node3D| { self_node.transform.scale = __t_other_scale; });
    }

}


impl ScriptObject for PuptestsNodeTestsTestNode3dApiPupScript {
    fn set_id(&mut self, id: NodeID) {
        self.id = id;
    }

    fn get_id(&self) -> NodeID {
        self.id
    }

    fn get_var(&self, var_id: u64) -> Option<Value> {
        VAR_GET_TABLE.get(&var_id).and_then(|f| f(self))
    }

    fn set_var(&mut self, var_id: u64, val: Value) -> Option<()> {
        VAR_SET_TABLE.get(&var_id).and_then(|f| f(self, val))
    }

    fn apply_exposed(&mut self, hashmap: &HashMap<u64, Value>) {
        for (var_id, val) in hashmap.iter() {
            if let Some(f) = VAR_APPLY_TABLE.get(var_id) {
                f(self, val);
            }
        }
    }

    fn call_function(
        &mut self,
        id: u64,
        api: &mut ScriptApi<'_>,
        params: &[Value],
    ) -> Value {
        DISPATCH_TABLE.get(&id).map_or(Value::Null, |f| f(self, params, api))
    }

    // Attributes

    fn attributes_of(&self, member: &str) -> Vec<String> {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn members_with(&self, attribute: &str) -> Vec<String> {
        ATTRIBUTE_TO_MEMBERS_MAP
            .get(attribute)
            .map(|members| members.iter().map(|s| s.to_string()).collect())
            .unwrap_or_default()
    }

    fn has_attribute(&self, member: &str, attribute: &str) -> bool {
        MEMBER_TO_ATTRIBUTES_MAP
            .get(member)
            .map(|attrs| attrs.iter().any(|a| *a == attribute))
            .unwrap_or(false)
    }
    
    fn script_flags(&self) -> ScriptFlags {
        ScriptFlags::new(1)
    }
}

// =========================== Static PHF Dispatch Tables ===========================

static VAR_GET_TABLE: phf::Map<u64, fn(&PuptestsNodeTestsTestNode3dApiPupScript) -> Option<Value>> =
    phf::phf_map! {
        1263699021464731510u64 => |script: &PuptestsNodeTestsTestNode3dApiPupScript| -> Option<Value> {
                        Some(json!(script.__t_spin))
                    },

    };

static VAR_SET_TABLE: phf::Map<u64, fn(&mut PuptestsNodeTestsTestNode3dApiPupScript, Value) -> Option<()>> =
    phf::phf_map! {
        1263699021464731510u64 => |script: &mut PuptestsNodeTestsTestNode3dApiPupScript, val: Value| -> Option<()> {
                            if let Some(v) = val.as_f64() {
                                script.__t_spin = v as f32;
                                return Some(());
                            }
                            None
                        },

    };

static VAR_APPLY_TABLE: phf::Map<u64, fn(&mut PuptestsNodeTestsTestNode3dApiPupScript, &Value)> =
    phf::phf_map! {

    };

static DISPATCH_TABLE: phf::Map<
    u64,
    fn(&mut PuptestsNodeTestsTestNode3dApiPupScript, &[Value], &mut ScriptApi<'_>) -> Value,
> = phf::phf_map! {
        432816268499948822u64 => | script: &mut PuptestsNodeTestsTestNode3dApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_transform_read_write(api);
            Value::Null
        },
        16102145252734054829u64 => | script: &mut PuptestsNodeTestsTestNode3dApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_rotation_assign_and_rotate(api);
            Value::Null
        },
        12614002744515878282u64 => | script: &mut PuptestsNodeTestsTestNode3dApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_rotation_set_equal_to_other(api);
            Value::Null
        },
        5299181515774658578u64 => | script: &mut PuptestsNodeTestsTestNode3dApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_for_loops_with_rotation(api);
            Value::Null
        },
        1038358772608182020u64 => | script: &mut PuptestsNodeTestsTestNode3dApiPupScript, params: &[Value], api: &mut ScriptApi<'_>| -> Value {
            script.__t_test_scale_and_position_assign(api);
            Value::Null
        },

    };
