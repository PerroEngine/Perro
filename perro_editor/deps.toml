# Optional script crate dependencies.
[dependencies]
toml = "0.9.12"
gltf = { version = "1.4", default-features = false }
# Typed render snapshots in editor integration tests; already an engine dependency.
perro_render_bridge = "0.1.0"
